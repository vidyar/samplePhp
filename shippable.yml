language: php

php:
  - 5.4
  
  
services:
  - mysql
  #- postgres
  - postgresql
  - elasticsearch
  - redis
  - memcache
  - mongo
  - rabbitmq
  - neo4j
  - couchdb
  - rethinkdb
  
build:
  ci:
    - php --version
    
  pre_ci_boot:
    image_name: shippable/minv2
    image_tag: latest
    pull: true
